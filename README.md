# Water_level_alarm_system
This experiment demonstrates a water level monitoring system using an Arduino nano. The system reads water level data from an analog water sensor, displays the current level and raw value on an I2C LCD (8x2), provides a visual indicator using an RGB LED, and triggers a buzzer when the water level reaches HIGH.
